# Copie para .streamlit/secrets.toml e ajuste a URL
# (secrets.toml não vai para o git — adicionar ao .gitignore)

API_URL = "http://localhost:8000"

# Em produção (ex: Railway, Render, Fly.io):
# API_URL = "https://sua-api.railway.app"
